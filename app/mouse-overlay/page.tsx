import MouseOverlay from "../components/MouseOverlay";

// Effect of mouse creating an overlay effect on top of elements (usually it inverts things)

const page = () => {
  return <MouseOverlay />;
};

export default page;
